# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '11.x'
  displayName: 'Install Node.js'

- script: |
    cd components/leabox-api
    mkdir output
  displayName: 'init environment'


- script: |
    npm install
    npm run build
  displayName: 'npm install and build'


- task: CopyFiles@2
  inputs:
    SourceFolder: '$(System.DefaultWorkingDirectory)/components/leabox-api'
    Contents: |
      dist/**/*.js
      dist/**/*.js.map
      package.json
      leabox-api.PublishSettings
    TargetFolder: './output'

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: './output'
    includeRootFolder: false
    archiveType: 'zip'
    archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip'
    replaceExistingArchive: true

- task: PublishBuildArtifacts@1
  inputs:
    ArtifactName: 'leabox-api'


# - task: CopyFiles@2
#   inputs:
#     SourceFolder: 'components/leabox-api/dist'
#     Contents: 'main.bundle.js*'
#     TargetFolder: '$(Build.ArtifactStagingDirectory)'
#     CleanTargetFolder: false
#     OverWrite: true

# - task: CopyFiles@2
#   inputs:
#     SourceFolder: 'components/leabox-api'
#     Contents: 'package*.json'
#     TargetFolder: '$(Build.ArtifactStagingDirectory)'
#     CleanTargetFolder: false
#     OverWrite: true

# - task: ArchiveFiles@2
#   inputs:
#     rootFolderOrFile: '$(Build.ArtifactStagingDirectory)'
#     includeRootFolder: false
#     archiveType: 'zip'
#     archiveFile: '$(Build.ArtifactStagingDirectory)/output/$(Build.BuildId).zip'
#     replaceExistingArchive: true

# - task: PublishBuildArtifacts@1
#   inputs:
#     PathtoPublish: '$(Build.ArtifactStagingDirectory)'
#     ArtifactName: 'leabox-api'
#     publishLocation: 'Container'